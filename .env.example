# Server
PORT=3001
NODE_ENV=development

# Database path (optional, defaults to ./server/db/mnemonic.db)
DB_PATH=./server/db/mnemonic.db

# Anthropic API key for AI card generation (optional — feature disabled without it)
ANTHROPIC_API_KEY=
