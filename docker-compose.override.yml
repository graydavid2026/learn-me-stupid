# Local dev overrides — hot reload, debug ports
services:
  app:
    build:
      context: .
      target: build
    command: npm run dev
    ports:
      - "3001:3001"
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
